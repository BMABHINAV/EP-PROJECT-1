# Mosquitto config for local development (Windows)
# Save to: C:\Program Files\mosquitto\mosquitto.conf
# Or run: mosquitto -c mosquitto_local.conf -v

listener 1883
allow_anonymous true

# Optional WebSocket listener for browser clients
listener 9001
protocol websockets
allow_anonymous true

log_type all
log_dest stdout
